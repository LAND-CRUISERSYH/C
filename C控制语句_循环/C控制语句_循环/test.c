#include<stdio.h>

int main()
{
	/*
	int num;
	int sum = 0;
	int status;
	printf("请输入一个整数: ");
	status = scanf("%d", &num);
	while (status == 1)
	{
		sum = sum + num;
		printf("请输入整数: ");
		status = scanf("%d", &num);
	}
	printf("最终相加的结果为: %d", sum);
	*/
	//printf("Hello world!\n");
	return 0;
}